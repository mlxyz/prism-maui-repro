<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:vm="clr-namespace:PrismApp.ViewModels"
             x:Class="PrismApp.Controls.CustomControl"
             prism:ViewModelLocator.AutowireViewModel="Automatic">
  <!-- This works: -->
  <!--<ContentView.BindingContext>
    <vm:CustomControlViewModel></vm:CustomControlViewModel>
  </ContentView.BindingContext>-->
  <VerticalStackLayout>
    <Label Text="This is the content:" VerticalOptions="Center" 
           HorizontalOptions="Center"></Label>
    <Label Text="{Binding CustomText}"
           VerticalOptions="Center" 
           HorizontalOptions="Center" />
  </VerticalStackLayout>
</ContentView>
